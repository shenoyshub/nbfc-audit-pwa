import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.icrats.audit.app',
  appName: 'nbfc-audit-pwa',
  webDir: 'dist/nbfc-audit-pwa/browser'
};

export default config;
